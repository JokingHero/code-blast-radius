All tests should pass after the succesfull implementation.

Test all files with:
```sh
cargo test
```
Test specific test e.g definitions_state_test.rs file with:
```sh
cargo test --test definitions_state_test -- --no-capture
```